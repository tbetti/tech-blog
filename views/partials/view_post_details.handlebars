<section class="card" data-id="{{id}}">
    <div class="card-head">
        Posted by {{user.dataValues.username}} at {{format_date updatedAt}}
    </div>
    <div class="card-body">
        {{post}}
    </div>
</section>